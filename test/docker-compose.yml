version: '3.7'

services:
  test-service-alpine:
    build:
      context: .
      dockerfile: Dockerfile-alpine

  test-service-debian:
    build:
      context: .
      dockerfile: Dockerfile-debian
